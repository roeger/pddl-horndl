(define (problem BTcat_problem)
(:domain BTcat)
(:init
       (bomb e)
       (cat i)
       (cat l)
       (cat m)
       (bomb o)
       (cat p)
       (contains g o)
       (contains j e)
       (contains f i)
       (contains h m)
       (contains b l)
       (contains n p))
(:goal (and (forall (?x - object) (AUX2 ?x)) (not (DATALOG_INCONSISTENT))))
)