(define (problem BTcat_problem)
(:domain BTcat)
(:init
       (cat a)
       (cat d)
       (cat g)
       (cat h)
       (bomb i)
       (cat n)
       (bomb o)
       (contains f o)
       (contains m i)
       (contains q n)
       (contains k h)
       (contains j a)
       (contains r d)
       (contains l g))
(:goal (and (forall (?x - object) (AUX2 ?x)) (not (DATALOG_INCONSISTENT))))
)